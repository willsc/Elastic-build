apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zookeeper-test.fullname" . }}-test-connection"
  labels:
    {{- include "zookeeper-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zookeeper-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
